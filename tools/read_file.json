{
    "name": "read_file",
    "description": "Read file",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "The path to the file to read."
            }
        },
        "required": ["path"]
    },
    "capsule": {
        "compute": "LOW",
        "ram": "64MB",
        "timeout": "10s",
        "max_retries": 1,
        "allowed_files": [{
            "path": ".",
            "mode": "read-only"
        }],
        "allowed_hosts": [],
        "env_variables": []
    }
}
